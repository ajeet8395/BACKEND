<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 p-10 text-white">
        <a class="text-blue-500 hover:underline" href="/">back to home</a>
        <h1 class="text-3xl tracking-tighter mb-3 mt-3">All Users</h1>
        <% if(users.length > 0){ %> 
            <div class="users flex gap-4 flex-wrap">
                <% users.forEach(function(user){ %>
                     <div class="user hover:scale-105 transition-all max-w-72 p-4 bg-zinc-800 rounded-lg">
                         <div class="w-full h-52 rounded-lg bg-zinc-500 overflow-hidden">
                             <img class="w-full h-full object-cover object-center" src="<%= user.image %>" alt="">
                         </div>
                         <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
                         <h5 class="text-zinc-500"><%= user.email %></h5>
                         <div class="flex gap-3 mt-8">
                             <a class="text-zinc-200" href="/edit/<%= user._id %>">Edit User</a>
                             <a class="text-red-500" href="/delete/<%=  user._id %>">Delete User</a>
                         </div>
                     </div>
                <% }) %>
             </div>
        <% } else { %> 
            <h3 class="text-zinc-500">No User Yet.</h3>
        <% } %>
    </div>
</body>
</html>